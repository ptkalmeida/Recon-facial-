# Dependencias de execucao do Face Recognition Pro 3.0
#
# Versoes verificadas em Python 3.14.3 (31/08/2026) - `pytest tests/` passa e o
# servidor sobe. O arquivo anterior pinava versoes de jan/2024 que nao instalam
# em Python 3.14 (numpy 1.26 nao tem wheel) e listava 9 pacotes que o codigo nao
# importa em lugar nenhum: pandas, alembic, scipy, scikit-learn, aiofiles,
# python-socketio, python-engineio, mtcnn, retina-face. Foram removidos.
#
# O backend de reconhecimento facial NAO esta aqui: e opcional e pesado.
# Ver requirements-recognition.txt - sem ele o sistema sobe em modo degradado
# (avisado no log de startup e em /api/health).

# --- Web ---
fastapi==0.136.0
uvicorn[standard]==0.44.0
python-multipart==0.0.26      # necessario para Form/File (upload de imagens)
jinja2==3.1.6                 # Jinja2Templates em main.py - antes vinha so de carona
                              # em dependencia transitiva, nunca declarado

# --- Configuracao e validacao ---
pydantic[email]==2.13.1       # extra [email] = email-validator, exigido por EmailStr
                              # nos schemas; tambem faltava ser declarado
pydantic-settings==2.13.1
python-dotenv==1.0.0
pyyaml==6.0.3

# --- Banco ---
sqlalchemy==2.0.49

# --- Seguranca ---
python-jose[cryptography]==3.5.0   # JWT (HS256)
passlib==1.7.4                     # interface de hashing
bcrypt==5.0.0                      # hash da senha admin + criptografia do embedding

# --- Visao computacional (deteccao base, sem modelo de reconhecimento) ---
# UMA unica distribuicao do OpenCV. Nao troque por opencv-python-headless nem
# instale as duas: ambas fornecem o modulo `cv2` e a que ganha o import e
# imprevisivel. O insightface (requirements-recognition.txt) declara
# `opencv-python`, entao esta e a variante que evita o conflito.
# Fixado na linha 4.x de proposito: o OpenCV 5.0 removeu cv2.CascadeClassifier
# das bindings, que o fallback de deteccao usa.
opencv-python==4.13.0.92
numpy==2.4.4
pillow==12.2.0

# --- Exportacao de relatorios ---
openpyxl==3.1.5    # Excel
reportlab==4.4.10  # PDF
